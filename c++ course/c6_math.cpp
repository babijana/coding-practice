#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    double x=9.4;
    double y=5;
    double z;
    //z=max(x,y);
    //z=min(x,y);
    //z=pow(x,4);
    //z=sqrt(y);
    //z=abs(-3);  :absolute value function
    //z=floor(x);

    cout<<z;
}